<%= form_with(model: bad_report, local: true) do |form| %>
  <% if bad_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bad_report.errors.count, "error") %> prohibited this bad_report from being saved:</h2>

      <ul>
        <% bad_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
